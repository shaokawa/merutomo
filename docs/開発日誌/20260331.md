## 2026-03-31 認証実装

### 今日やったこと

- Supabase PostgreSQL への接続確認
  - `DATABASE_URL` のユーザー名が誤っており、認証エラーになっていた
  - `backend/.env` の接続文字列を修正して接続成功

- Supabase Auth を使う方針に変更
  - 自前のメモリ認証ではなく、Supabase Auth を使う構成に切り替えた
  - `SUPABASE_URL`, `SUPABASE_ANON_KEY` を設定で読めるようにした

- user登録APIを実装
  - `POST /auth/register` から Supabase の `/auth/v1/signup` を呼ぶようにした
  - `backend/internal/auth/supabase_client.go` を追加
  - `backend/internal/auth/service.go` の `Register` を Supabase 経由に変更

- login API を実装
  - `POST /auth/login` から Supabase の `/auth/v1/token?grant_type=password` を呼ぶようにした
  - ログイン成功時に access token と user を返すようにした

- me API を実装
  - `GET /auth/me` で Bearer token を Supabase に渡してユーザー取得するようにした
  - `Authorization: Bearer <token>` で認証確認できる状態になった

- logout API を追加
  - `POST /auth/logout` を追加
  - Supabase の `/auth/v1/logout` を呼ぶ `SignOut` を実装した

- ルーターテストを修正
  - `auth.Handler` が `*Service` 固定ではなく interface を受け取るように変更
  - `router_test.go` を fake service ベースに修正
  - `/auth/logout` のテストも追加
  - `go test ./...` が通るところまで確認

### ハマった点

- `.env` の読み込み場所
  - `go run ./cmd/api` を `backend` で実行すると `backend/.env` が読まれる
  - ルートの `.env` と二重管理になるので今後整理が必要

- Supabase Auth の signup エラー切り分け
  - 最初は API 側が `500 failed to register user` に握りつぶしていて原因が見えなかった
  - `SupabaseAPIError` を入れて status/body を返すようにして切り分けしやすくした

- 開発中の Supabase Auth 制約
  - `example.com` は `email_address_invalid` で弾かれた
  - 短時間の連続 signup で `over_email_send_rate_limit` が返った
  - メール確認前の login は `email_not_confirmed` になった

### 今日の到達点

- `register -> confirm email -> login -> me` の最小認証フローは確認済み
- `logout` のコードとルートは追加済み
- テストは通っている

### 次回やること

- フロント連携を考える
  - UI構築
  - login後の token 保存場所
  - logout時の token 破棄
  - 確認メール後のリダイレクト設計

## Go初期化手順

最初のゴールは、`backend` で `Gin` が立ち上がり、`GET /health` が `200 OK` を返すところまでです。そこまでできれば、次に認証やDB接続を積めます。

**手順**
1. `backend` に入る
```bash
cd /Users/shiokawataiga/Desktop/go/merutomo/backend
```

2. Goモジュールを初期化する  
モジュール名は、あとでGitHubに載せるならリポジトリ準拠にしておくのが無難です。
```bash
go mod init github.com/あなたのGitHub名/merutomo/backend
```
ローカル確認だけなら仮で
```bash
go mod init merutomo/backend
```
でも進められます。

3. Ginを入れる
```bash
go get github.com/gin-gonic/gin
```

4. 最低限のディレクトリを作る
```bash
mkdir -p cmd/api internal/router
```

5. `cmd/api/main.go` を作る  
中身はこれで十分です。
```go
package main

import (
	"log"

	"github.com/gin-gonic/gin"
	"merutomo/backend/internal/router"
)

func main() {
	r := gin.Default()
	router.Setup(r)

	log.Println("server started on :8080")
	if err := r.Run(":8080"); err != nil {
		log.Fatal(err)
	}
}
```

6. `internal/router/router.go` を作る
```go
package router

import "github.com/gin-gonic/gin"

func Setup(r *gin.Engine) {
	r.GET("/health", func(c *gin.Context) {
		c.JSON(200, gin.H{
			"status": "ok",
		})
	})
}
```

7. import path を自分の `go mod init` に合わせて直す  
もし `go mod init github.com/xxx/merutomo/backend` にしたなら、`main.go` のこの行:
```go
"merutomo/backend/internal/router"
```
を
```go
"github.com/xxx/merutomo/backend/internal/router"
```
に変えてください。

8. 起動する
```bash
go run ./cmd/api
```

9. 動作確認する
別ターミナルで:
```bash
curl http://localhost:8080/health
```
期待する返り値:
```json
{"status":"ok"}
```

**ここで作られる構成**
```text
backend/
  go.mod
  go.sum
  cmd/
    api/
      main.go
  internal/
    router/
      router.go
```

**補足**
- `cmd/api` はアプリの起動入口です。
- `internal` は外部から直接使わせたくないアプリ内部実装を置く場所です。
- 今は `router` だけ分ければ十分です。認証、DB、設定は次に切ります。

ここまで自分でやってみてください。終わったら次に、`config` と `.env` を入れて「環境変数を読むAPIサーバ」に進めます。
